node_modules/
.svelte-kit/
build/
.env
.env.*
!.env.example
.DS_Store
*.log
.vite/
dev-dist/

# Global gitignore has `Icon?` (macOS Finder cruft) which matches our PWA
# icon files case-insensitively. Re-include them.
!static/icons/
!static/icons/*.png

# Personal historical-game OCR data + the derived seed bundle. Lives on the
# user's machine only; never committed to the repo.
history.nogit/
